# Furry Image Studio bridge (disabled)

Bunch keeps an atomic boundary from Furry Image Studio.

- Runtime hooks for Furry Image Studio transforms and keeper uploads are
  explicitly disabled.
- Use Bunch-native flows (`generate_scene`, private gallery uploads, and
  appearance-reference selection) instead of any Furry Image Studio adapter.
