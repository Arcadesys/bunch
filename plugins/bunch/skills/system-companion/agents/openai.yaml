interface:
  display_name: "System Companion"
  short_description: "Private notes, switches, photos, and alter scene generation"
  default_prompt: "Use $system-companion to review my private System state. When I ask to draw named alters, call generate_scene directly."
dependencies:
  tools:
    - type: "mcp"
      value: "system-companion"
      description: "Authenticated private System MCP server"
      transport: "streamable_http"
      url: "https://system.thearcades.me/mcp"
policy:
  allow_implicit_invocation: true
