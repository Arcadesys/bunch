// RFC 9728 path-aware discovery form for the /mcp resource.
// Keep this alias in sync with the origin-level metadata route because clients
// differ in which protected-resource well-known URL they probe first.
export { GET } from "../route";
