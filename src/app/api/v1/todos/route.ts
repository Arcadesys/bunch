import { NextResponse } from "next/server";
import { apiOwner, apiResponse, idempotencyKey, jsonBody, mutationMeta, parseBoolean, parseLimit, requireSameOrigin, splitValues } from "@/server/http-api";
import { getSystemService } from "@/server/system-service";
export const runtime = "nodejs";
export async function GET(request: Request) { return apiResponse(async () => { const ownerId = await apiOwner(request); const q = new URL(request.url).searchParams; const page = await getSystemService().listTodos(ownerId, { cursor: q.get("cursor") ?? undefined, limit: parseLimit(q.get("limit")), includeArchived: parseBoolean(q.get("includeArchived")), status: splitValues(q.get("status")) as never, assigneeAlterId: q.get("assigneeAlterId") ?? undefined, dueFrom: q.get("dueFrom") ?? undefined, dueTo: q.get("dueTo") ?? undefined, priority: splitValues(q.get("priority")) as never, coverageId: q.get("coverageId") ?? undefined }); return NextResponse.json({ data: page.data, meta: { nextCursor: page.nextCursor } }); }); }
export async function POST(request: Request) { return apiResponse(async () => { requireSameOrigin(request); const ownerId = await apiOwner(request); const requestId = idempotencyKey(request); const result = await getSystemService().createTodo(ownerId, { ...(await jsonBody(request)), requestId } as never, "WEB"); return NextResponse.json({ data: result.data, meta: mutationMeta(requestId, result.replayed) }, { status: result.replayed ? 200 : 201 }); }); }
