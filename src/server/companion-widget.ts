export function catchUpWidget(publicOrigin: string) {
  const safeOrigin = JSON.stringify(publicOrigin);
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="referrer" content="no-referrer">
  <title>Bunch catch-up</title>
  <style>
    :root{color-scheme:dark;font:20px/1.5 Arial,Helvetica,sans-serif;color:#f8fafc;background:#07111f}
    *{box-sizing:border-box}body{margin:0;padding:18px;background:#07111f;color:#f8fafc}.shell{max-width:780px;margin:auto;border:3px solid #e2e8f0;background:#0b1626}.top{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:16px;padding:18px 20px;border-bottom:3px solid #e2e8f0}.brand{font-size:1.4rem;font-weight:900}.link,.button,button,select,input{font:inherit}.link{color:#f8fafc;font-weight:800;text-underline-offset:4px}.hero{display:grid;grid-template-columns:104px 1fr;gap:18px;margin:16px;border:3px solid #e2e8f0;padding:20px}.avatar{width:104px;height:104px;display:grid;place-items:center;border:3px solid #38bdf8;background:#102a43;font-size:1.7rem;font-weight:900}.hero-copy{min-width:0}.hero h1{font-size:clamp(2rem,8vw,3rem);line-height:1.05;margin:0 0 12px}.hero p{margin:6px 0}.identity{color:#38bdf8;font-weight:900}.progress{grid-column:1 / -1;margin-top:6px;border:3px solid #e2e8f0;padding:14px}.progress-row{display:flex;justify-content:space-between;gap:12px;font-weight:800}.progress-track{height:14px;border:2px solid #e2e8f0;margin-top:8px}.progress-bar{height:100%;background:#38bdf8;transition:width .2s ease}.notice{margin:0;padding:14px 20px;border-top:3px solid #e2e8f0;border-bottom:3px solid #e2e8f0;background:#102a43;font-weight:800}.tabs{display:grid;grid-template-columns:repeat(3,1fr);border-bottom:3px solid #e2e8f0}.tab{min-height:54px;border:0;border-right:2px solid #e2e8f0;background:#07111f;color:#f8fafc;font-weight:900;cursor:pointer}.tab:last-child{border-right:0}.tab[aria-pressed="true"]{background:#38bdf8;color:#04131c}.content{padding:22px 20px}.section-title{font-size:1.55rem;line-height:1.15;margin:0 0 6px}.section-copy{margin:0 0 18px;color:#cbd5e1}.item{padding:22px 0;border-top:3px solid #e2e8f0}.item:first-of-type{border-top:0}.type{display:inline-block;border:2px solid #e2e8f0;padding:3px 8px;font-size:1rem;font-weight:900;letter-spacing:.04em}.item h3{font-size:1.35rem;line-height:1.15;margin:10px 0}.meta{color:#cbd5e1;margin:7px 0}.why{font-weight:700;margin:14px 0}.next{border-left:7px solid #38bdf8;background:#102a43;padding:10px 12px;margin:14px 0}.reviewed{opacity:1}.state{font-weight:900;color:#38bdf8}.actions{display:grid;grid-template-columns:repeat(2,1fr);gap:10px;margin-top:16px}.button,button{min-height:54px;border:3px solid #e2e8f0;background:#38bdf8;color:#04131c;padding:10px 12px;font-weight:900;cursor:pointer}.secondary{background:#0b1626;color:#f8fafc}.defer-fields{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:10px}[hidden]{display:none!important}select,input{min-height:52px;width:100%;border:3px solid #e2e8f0;background:#07111f;color:#f8fafc;padding:8px}.thread-link{display:inline-block;margin-top:10px;color:#38bdf8;font-weight:900}.quick{border-top:3px solid #e2e8f0;padding:22px 20px}.quick h2{margin:0 0 14px}.quick-actions{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.empty{border:3px dashed #e2e8f0;padding:18px;font-weight:800}button:focus-visible,.link:focus-visible,select:focus-visible,input:focus-visible,.thread-link:focus-visible{outline:5px solid #fbbf24;outline-offset:3px}button:disabled{opacity:.55;cursor:wait}
    @media(max-width:560px){body{padding:0}.shell{border-width:0}.top,.content,.quick{padding-left:16px;padding-right:16px}.hero{grid-template-columns:80px 1fr;margin:12px;padding:14px}.avatar{width:80px;height:80px;font-size:1.35rem}.actions,.quick-actions{grid-template-columns:1fr}.defer-fields{grid-template-columns:1fr}.tabs{grid-template-columns:1fr}.tab{font-size:1rem;border-right:0;border-bottom:2px solid #e2e8f0}.hero{grid-template-columns:1fr}}
    @media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}
  </style>
</head>
<body>
<main class="shell">
  <header class="top"><span class="brand">Bunch</span><a class="link" id="open-site" href="${publicOrigin}">Open full website</a></header>
  <section class="hero" aria-labelledby="welcome"><div class="avatar" id="avatar" aria-hidden="true">?</div><div class="hero-copy"><h1 id="welcome">Your catch-up</h1><div id="front"></div></div><div class="progress" id="progress"></div></section>
  <label>Catch-up period<select id="period-choice"><option value="">Choose a current fronter</option></select></label><p class="notice" id="notice" role="status">Loading your catch-up…</p>
  <div class="tabs" role="group" aria-label="Filter this catch-up"><button class="tab" aria-pressed="true" data-filter="ALL">All catch-up items</button><button class="tab" aria-pressed="false" data-filter="TODO">Catch-up tasks</button><button class="tab" aria-pressed="false" data-filter="NOTE">Catch-up notes</button></div>
  <section class="content" aria-labelledby="items-heading"><h2 class="section-title" id="items-heading">Items to review</h2><p class="section-copy">These filters show only this catch-up. Review actions do not complete tasks or change saved notes.</p><div id="items"></div></section>
  <section class="quick" aria-labelledby="quick-heading"><h2 id="quick-heading">Quick actions</h2><div class="quick-actions"><button class="secondary" data-prompt="Help me leave a note in Bunch.">Leave a note</button><button class="secondary" data-prompt="Help me add a todo in Bunch.">Add a todo</button><button class="secondary" data-prompt="Offer to save this thread as important. Do not save it until I confirm the title, approved summary, key decision or action, and recipients.">Save this thread</button></div></section>
</main>
<script>
const SITE=${safeOrigin};const pending=new Map();let callId=1;let state=null;let filter='ALL';let presence=null;let generation=0;
const byId=id=>document.getElementById(id);const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[char]));
function toolError(result){const detail=Array.isArray(result?.content)?result.content.filter(part=>part.type==='text'&&typeof part.text==='string').map(part=>part.text).join(' '):'';return new Error((detail||'Unable to complete this action. Please try again.').replace(/[\\u0000-\\u001f\\u007f]/g,' ').slice(0,500));}
function checkedResult(result){if(result?.isError)throw toolError(result);return result;}
function callTool(name,args){if(window.openai?.callTool)return Promise.resolve(window.openai.callTool(name,args)).then(checkedResult);return new Promise((resolve,reject)=>{const id=callId++;pending.set(id,{resolve,reject});parent.postMessage({jsonrpc:'2.0',id,method:'tools/call',params:{name,arguments:args}},'*');});}
function unwrap(value){if(value?.structuredContent?.catchUp!==undefined)return value.structuredContent.catchUp;if(value?.structuredContent?.data?.items)return value.structuredContent.data;if(value?.structuredContent?.items)return value.structuredContent;if(value?.data?.items)return value.data;if(value?.catchUp!==undefined)return value.catchUp;if(value?.items)return value;return null;}
function initials(name){return String(name||'?').trim().split(/\\s+/).slice(0,2).map(part=>part[0]||'').join('').toUpperCase()||'?';}
function reviewLabel(value){return ({NEW:'Not reviewed',ACKNOWLEDGED:'Reviewed',DEFERRED:'Review postponed',RESOLVED:'Review finished'})[value]||'Review state unavailable';}
function time(value){return new Intl.DateTimeFormat(undefined,{dateStyle:'medium',timeStyle:'short'}).format(new Date(value));}
function date(value){return new Intl.DateTimeFormat(undefined,{dateStyle:'medium'}).format(new Date(value+'T12:00:00'));}
function progress(){if(!state)return '';const percent=state.totalCount?Math.round(state.reviewedCount/state.totalCount*100):100;return '<div class="progress-row"><span>'+state.reviewedCount+' of '+state.totalCount+' reviewed</span><span>'+percent+'%</span></div><div class="progress-track" role="progressbar" aria-label="Catch-up reviewed" aria-valuemin="0" aria-valuemax="'+state.totalCount+'" aria-valuenow="'+state.reviewedCount+'"><div class="progress-bar" style="width:'+percent+'%"></div></div>';}
function itemCard(item){const reviewed=item.reviewState!=='NEW';const thread=item.itemType==='THREAD';const defer=item.deferUntilNextSwitch?'Returns at next recorded period':item.deferUntil?'Returns '+time(item.deferUntil):'';return '<article class="item '+(reviewed?'reviewed':'')+'" data-entry="'+item.entryId+'"><span class="type">'+esc(item.itemType)+(thread&&item.threadSource?' · '+esc(item.threadSource):'')+'</span><h3>'+esc(item.title)+'</h3><p class="meta">From '+esc(item.fromLabel)+' · To '+esc(item.toLabel)+' · '+esc(time(item.timestamp))+'</p>'+(item.statusLabel||item.dueOn?'<p class="meta">'+esc(item.statusLabel||'')+(item.dueOn?' · Due '+esc(date(item.dueOn)):'')+'</p>':'')+'<p class="why">'+esc(item.whyItMatters)+'</p><p class="next"><strong>Next:</strong> '+esc(item.nextAction)+'</p>'+(thread&&item.threadUrl?'<a class="thread-link" href="'+esc(item.threadUrl)+'" data-external="true">Open saved thread</a>':'')+'<p class="state">'+esc(reviewLabel(item.reviewState))+(defer?' · '+esc(defer):'')+'</p><div class="defer-fields" data-defer-fields hidden><label><span>Return time</span><select data-defer-choice><option value="TODAY">Later today</option><option value="TOMORROW">Tomorrow</option><option value="NEXT_SWITCH">Next recorded period</option><option value="CUSTOM">Custom</option></select></label><label data-custom-label hidden><span>Custom time</span><input data-custom-time type="datetime-local"></label><button data-action="DEFERRED" data-version="'+item.version+'">Postpone review</button></div><div class="actions"><button data-action="ACKNOWLEDGED" data-version="'+item.version+'">Mark reviewed</button><button class="secondary" data-show-defer>Review later</button></div></article>';}
function render(next){if(next?.isError){byId('notice').textContent=toolError(next).message;return;}const incoming=next?.structuredContent||next;presence=incoming?.presence||presence;const periods=[...(presence?.fronting||[])].filter(Boolean);byId('period-choice').innerHTML='<option value="">Choose a current fronter</option>'+periods.map(p=>'<option value="'+p.id+'">'+esc(p.alterName)+' · '+(p.kind==='HOSTING'?'hosting':'fronting')+'</option>').join('');state=unwrap(next);byId('period-choice').value=state?.presencePeriodId||'';if(!state){byId('welcome').textContent='Your catch-up';byId('avatar').textContent='?';byId('front').innerHTML='<p>No catch-up is available.</p>';byId('progress').innerHTML='';byId('items').innerHTML='<p class="empty">Open Bunch to check the hosting and fronting records, then choose a catch-up period.</p>';byId('notice').textContent='No catch-up is open.';return;}byId('welcome').textContent='Catch-up for '+state.alterName;byId('avatar').textContent=initials(state.alterName);byId('front').innerHTML='<p><span class="identity">Catch-up recipient:</span> '+esc(state.alterName)+'</p><p>'+(state.firstTime?'Previous fronting end unknown · available records':'Catch-up window: '+(state.windowStart?esc(time(state.windowStart))+' to ':'through ')+esc(time(state.windowEnd)))+'</p>';byId('progress').innerHTML=progress();const visible=state.items.filter(item=>filter==='ALL'||item.itemType===filter);const regular=visible.filter(item=>item.itemType!=='THREAD'),threads=visible.filter(item=>item.itemType==='THREAD');let html=regular.map(itemCard).join('');if(threads.length)html+='<h2 class="section-title">Important threads</h2>'+threads.map(itemCard).join('');byId('items').innerHTML=html||'<p class="empty">No items in this catch-up match this filter.</p>';byId('notice').textContent=state.totalCount-state.reviewedCount+' item'+(state.totalCount-state.reviewedCount===1?'':'s')+' still new.';bind();}
function deferArgs(card){const choice=card.querySelector('[data-defer-choice]').value;if(choice==='NEXT_SWITCH')return{deferUntilNextSwitch:true};const now=new Date();if(choice==='TODAY')now.setHours(now.getHours()+4);if(choice==='TOMORROW'){now.setDate(now.getDate()+1);now.setHours(9,0,0,0);}if(choice==='CUSTOM'){const custom=card.querySelector('[data-custom-time]').value;if(!custom)throw new Error('Choose a custom return time.');return{deferUntil:new Date(custom).toISOString()};}return{deferUntil:now.toISOString()};}
async function update(card,button){try{document.querySelectorAll('button').forEach(control=>control.disabled=true);byId('notice').textContent='Saving review state…';const action=button.dataset.action;const extra=action==='DEFERRED'?deferArgs(card):{};const result=await callTool('set_catch_up_item_state',{entryId:card.dataset.entry,expectedVersion:Number(button.dataset.version),state:action,requestId:crypto.randomUUID(),...extra});render(result);}catch(error){byId('notice').textContent=error?.message||'Unable to save review state.';}finally{document.querySelectorAll('button').forEach(control=>control.disabled=false);}}
function bind(){document.querySelectorAll('[data-action]').forEach(button=>button.onclick=()=>update(button.closest('.item'),button));document.querySelectorAll('[data-show-defer]').forEach(button=>button.onclick=()=>{const fields=button.closest('.item').querySelector('[data-defer-fields]');fields.hidden=!fields.hidden;});document.querySelectorAll('[data-defer-choice]').forEach(select=>select.onchange=()=>{select.closest('[data-defer-fields]').querySelector('[data-custom-label]').hidden=select.value!=='CUSTOM';});document.querySelectorAll('[data-external]').forEach(link=>link.onclick=event=>{if(window.openai?.openExternal){event.preventDefault();window.openai.openExternal({href:link.href});}});}
async function refresh(){try{const p=await callTool('get_current_presence',{});presence=p?.structuredContent?.data||p?.data;render(await callTool('get_catch_up',{}));}catch(error){byId('notice').textContent=error?.message||'Unable to load catch-up.';}}
byId('period-choice').onchange=async event=>{const periodId=event.target.value;const current=++generation;if(!periodId){render({catchUp:null,presence});return;}byId('items').innerHTML='';byId('notice').textContent='Loading selected catch-up…';try{const result=await callTool('get_catch_up',{periodId});if(current===generation)render(result);}catch(error){if(current===generation)byId('notice').textContent=error?.message||'Unable to load catch-up.';}};
document.querySelectorAll('[data-filter]').forEach(tab=>tab.onclick=()=>{filter=tab.dataset.filter;document.querySelectorAll('[data-filter]').forEach(other=>other.setAttribute('aria-pressed',String(other===tab)));render(state);});
document.querySelectorAll('[data-prompt]').forEach(button=>button.onclick=()=>{const prompt=button.dataset.prompt;if(window.openai?.sendFollowUpMessage)window.openai.sendFollowUpMessage({prompt});else parent.postMessage({jsonrpc:'2.0',method:'ui/message',params:{role:'user',content:[{type:'text',text:prompt}]}},'*');});
byId('open-site').onclick=event=>{if(window.openai?.openExternal){event.preventDefault();window.openai.openExternal({href:SITE});}};
window.addEventListener('message',event=>{if(event.source!==window.parent)return;const message=event.data;if(message?.id&&pending.has(message.id)){const promise=pending.get(message.id);pending.delete(message.id);if(message.error)promise.reject(message.error);else{try{promise.resolve(checkedResult(message.result));}catch(error){promise.reject(error);}}return;}if(message?.method==='ui/notifications/tool-result')render(message.params);});
if(window.openai?.toolOutput)render(window.openai.toolOutput);else refresh();
</script>
</body>
</html>`;
}
